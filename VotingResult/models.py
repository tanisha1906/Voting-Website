from django.db import models
# from VotingWebsite.models import Election

# Create your models here.
#Settings/Configuration Model: This could include various settings related to the 
#application's behavior, such as voting rules, maximum number of candidates per election, etc.


# class Analytics(models.Model):
#     election = models.ForeignKey('Election', on_delete=models.CASCADE)
#     total_votes = models.PositiveIntegerField(default=0)
#     voter_turnout_percentage = models.FloatField(default=0)

